pdfplumber==0.11.4
sentence-transformers==3.3.1
numpy==1.26.4

faiss-cpu==1.8.0
pymongo==4.8.0
networkx==3.3
python-dotenv==1.0.1

google-generativeai==0.8.3
pydantic==2.8.2
Flask==3.0.3
flask-cors==5.0.0

redis==5.0.8           # Redis client (query + retrieval cache, job status)
pika==1.3.2            # RabbitMQ sync client (worker consumer + publisher)

# ── Optional / future ──────────────────────────────────────────────────────────
# aio-pika==9.4.3      # async RabbitMQ (upgrade path if API moves to async)
# fastapi==0.115.0
# uvicorn[standard]==0.30.6
# crewai==0.80.0
# pytesseract==0.3.13
# Pillow==10.4.0
